package fr.free.chiquichi.server;

public class Mail {
	/*
	 * import java.util.Properties;
	 * 
	 * import javax.mail.Message; import javax.mail.MessagingException; import
	 * javax.mail.Session; import javax.mail.Transport; import
	 * javax.mail.internet.AddressException; import
	 * javax.mail.internet.InternetAddress; import
	 * javax.mail.internet.MimeMessage;
	 * 
	 * // ... Properties props = new Properties(); Session session =
	 * Session.getDefaultInstance(props, null);
	 * 
	 * String msgBody = "...";
	 * 
	 * try { Message msg = new MimeMessage(session); msg.setFrom(new
	 * InternetAddress("admin@example.com", "Example.com Admin"));
	 * msg.addRecipient(Message.RecipientType.TO, new
	 * InternetAddress("user@example.com", "Mr. User"));
	 * msg.setSubject("Your Example.com account has been activated");
	 * msg.setText(msgBody); Transport.send(msg);
	 * 
	 * } catch (AddressException e) { // ... } catch (MessagingException e) { //
	 * ... }
	 * http://code.google.com/intl/fr/appengine/docs/java/datastore/overview
	 * .html
	 * http://code.google.com/intl/fr/webtoolkit/doc/latest/RefWidgetGallery
	 * .html http://code.google.com/intl/fr/appengine/docs/java/config/cron.html
	 * http://www.vogella.de/articles/GWT/article.html#uibinder
	 *//*
		 * Query query = pm.newQuery(FeedEntity.class);
		 * query.setFilter("lastUpdateDate>= dateParam");
		 * query.setOrdering("lastUpdateDate desc");
		 * query.declareParameters("Date dateParam"); query.setRange(0,10);
		 * 
		 * List<FeedEntity> results = (List<FeedEntity>) query.execute(new
		 * Date());
		 */
}
